name: SFTP-Deploy
on:
  push:
    branches:
      - sandbox
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: deploy
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
          username: '${{ secrets.FTP_USER_SANDBOX }}'
          server: '${{ secrets.FTP_HOST_SANDBOX }}'
          port: 22
          ssh_private_key: ${{ secrets.SSH_PRIVATE_KEY_SANDBOX }}
          sftp_only: true
          local_path: './*'
          remote_path: '/public_html/'